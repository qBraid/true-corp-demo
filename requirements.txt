# "Which cells can sleep tonight?" — demo dependencies
# Python >= 3.10.  Runs on qBraid Lab with pre-configured credentials.
# Do NOT add boto3 / AWS keys — device access goes through qBraid.
qbraid>=0.11.0          # API V2; earlier versions are incompatible
amazon-braket-sdk>=1.83.0
networkx>=3.0
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
Pillow>=10.0            # asset handling for slide/map exports
ipywidgets>=8.1         # interactive result widgets
anywidget>=0.11         # backs Plotly FigureWidget
plotly>=6.0             # dark-themed result widgets + deck figures
kaleido                 # static PNG export (dev / slide rendering)
