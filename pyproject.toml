[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]
# Notebooks are normalised with nbformat, not reformatted by black, so their
# carefully aligned teaching cells stay as written.
extend-exclude = '''
/(
    \.venv
  | qBraid
  | data
  | results
  | resources
)/
'''
